<!-- 自定义拦截器,可多个 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.ioc.interceptor.AddTimeInterceptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>

注意当开启<mvc:annotation-driven/>时普通的配置拦截器将失效